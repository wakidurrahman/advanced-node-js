// import all the members of the module
/**
 * Using the `*` syntax (also called `namespace import`) to import all the members of the module
 * and assign them to the local loggerModule variable.
 */
import * as loggerModule from './logger.js';

console.log(loggerModule);
